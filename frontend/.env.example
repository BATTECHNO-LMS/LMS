# Backend API origin (no trailing slash, no /api suffix)
VITE_API_BASE_URL=https://lms-vnto.onrender.com

# Published SPA origins (comma-separated). Backend CORS_ORIGINS must include every origin the browser uses.
VITE_APP_ORIGINS=https://lms.battechno.com,https://www.lms.battechno.com
