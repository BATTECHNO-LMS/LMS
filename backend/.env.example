NODE_ENV=development
PORT=4000

# PostgreSQL — use your local or cloud connection string
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/battechno_lms?schema=public"

# JWT — generate a strong secret for production
JWT_SECRET=
JWT_EXPIRES_IN=7d

# File storage (relative to backend/ or absolute)
UPLOAD_DIR=uploads
