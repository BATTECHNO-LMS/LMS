NODE_ENV=development
PORT=4000

# PostgreSQL — copy to backend/.env (never commit .env)
# Neon: pooled host often ends in -pooler.*.neon.tech — use sslmode=require
# If the database already has tables/views, prefer `npx prisma db pull` over `db push`
DATABASE_URL="postgresql://neondb_owner:npg_OnAyUBgfq7G8@ep-spring-brook-amewymzq-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# JWT — generate a strong secret for production
JWT_SECRET=
JWT_EXPIRES_IN=7d

# File storage (relative to backend/ or absolute)
UPLOAD_DIR=uploads
